<resources>
    <!-- Base theme; the real colors come from Compose (see ui/theme). Uses a
         Material3 no-action-bar theme so Compose owns the whole window. -->
    <style name="Theme.UnoP2P" parent="android:Theme.Material.NoActionBar">
        <item name="android:statusBarColor">#0E1116</item>
        <item name="android:navigationBarColor">#0E1116</item>
        <item name="android:windowBackground">#0E1116</item>
    </style>
</resources>
